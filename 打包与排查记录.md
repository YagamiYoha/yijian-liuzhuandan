# 打包与排查记录（2026-08-31 会话存档）

> 本文件记录"把程序打包成 Windows exe"的过程、结论和**待排查问题**，供下次继续。
> 配套存档：`项目进度.md`（总体进度）、`README.md`（使用说明）。源码在 `dist_一键流转单源码.zip`。

---

## 一、打包结果（已成功产出 exe）

- **exe 位置**：`C:\Users\armstrong\form_tool\dist\一键流转单.exe`（约 302MB）
- **构建目录**：`C:\Users\armstrong\form_tool\`（源码解压处 + venv）
- **构建环境**：Windows 宿主（WSL2，机器名 DESKTOP-RQ0BBKO），Python 3.12.1（`C:\Users\armstrong\AppData\Local\Programs\Python\Python312`），venv 在 `form_tool\venv`
- **依赖版本**：PySide6 6.11.2、rapidocr_onnxruntime 1.4.4、onnxruntime 1.29.0、pyinstaller 6.22.2、python-docx 1.2.0、openpyxl 3.1.5、flask 3.1.3
- **打包命令**（等价 build_windows.bat）：
  ```
  venv\Scripts\python.exe -m PyInstaller --noconfirm --clean --onefile --windowed --name 一键流转单 ^
    --add-data "static;static" --collect-all rapidocr_onnxruntime --collect-all onnxruntime ^
    --hidden-import PySide6.QtWebEngineWidgets --hidden-import PySide6.QtWebEngineCore main.py
  ```

## 二、部署要求（重要！）

exe 采用"**配置文件外置**"设计，运行时**必须在 exe 同目录**放：
- `config.json`（字段/模板路径配置，可改）
- `templates\` 文件夹（Word/Excel 模板）
- （`uploads\`、`output\` 会自动创建）

**第一次运行崩了就是因为缺这些**：弹窗报
`FileNotFoundError: 'C:\Users\armstrong\form_tool\dist\config.json'`。
把 config.json 和 templates 复制到 dist 后，exe 正常启动（`/`、`/api/config` 均 200，程序窗口正常弹出）。

> 已在 build_windows.bat 第 4 步加入"自动复制 config.json/templates 到 dist"，下次打包不再需要手动复制。
> 部署时把**整个 dist 文件夹**拷到内网电脑即可。

## 三、❌ 待排查问题：OCR 接口导致 exe 崩溃

**现象**（本次实测，打包后的 exe）：
1. `GET /` → 200；`GET /api/config` → 200 ✅
2. `POST /api/ocr`（上传测试图片）→ **HTTP 502 Bad Gateway**，随后 exe **进程直接消失**（崩溃退出），8090 端口不再响应，output 目录为空。

**已排除**：不是"缺 config.json"（基础接口正常）；不是模型未打包（还没走到模型）。

**疑点/待验证方向**（下次继续按此排查）：
1. **models 是否真的进了 exe**：`--collect-all rapidocr_onnxruntime` 理论上把 `rapidocr_onnxruntime/models/*.onnx` 打进 `_MEIPASS`。需验证冻结后模型路径能否被 RapidOCR() 找到（可在 exe 里加日志打印模型路径）。
2. **崩溃类型**：进程消失（非优雅 500）更像**原生崩溃（段错误）**，可能来自 onnxruntime/opencv 在冻结环境下的 DLL 问题。已加 **faulthandler**（main.py），下次崩溃会把线程栈写进 exe 旁的 `崩溃日志.log`，一眼可见。
3. **rapidocr 1.4.4 在 PyInstaller 下的已知问题**：可试降级 `rapidocr_onnxruntime==1.3.2` 或改用 `--collect-all onnxruntime` 之外的方式（如显式 `--add-binary` onnxruntime DLL）。
4. **线程/事件循环**：Flask 服务跑在后台线程，OCR 首次加载模型耗时 10~30s，期间与 PySide6 主线程交互可能有问题；可先把 OCR 改成独立进程或加锁（已有线程锁）。
5. **用控制台版验证**：临时打包 `--console`（非 --windowed）版本，跑 OCR 看 stderr 报错，是最直接的定位手段。

**本次排查操作**（已做）：
- 源码已加：`main.py` faulthandler + 崩溃日志（exe 旁 `崩溃日志.log`）、config 缺失友好弹窗；`app.py` OCR 异常写日志。**下次构建的 exe 崩溃时会在 exe 旁留日志**。
- 测试图片：`测试/_ocr_test.png`（中文工程铭牌样式，含"工程名称/施工单位/联系电话/日期"）。

## 四、环境事实（本机 WSL2 相关，下次要用）

- **代理**：CordCloud，`127.0.0.1:7890`（http/socks5 均可），从 WSL 可直连（镜像网络模式）。大文件下载走它最快。
- **pip 大包**：清华镜像 `-i https://pypi.tuna.tsinghua.edu.cn/simple/` 或默认源+代理都行。
- **Windows Python**：3.12（可用，已验证打包）+ 3.14（默认 py 启动器，太新别用）。
- **GUI 界面服务**：`node /usr/local/bin/dsh web --port 3081 --no-open`，重启脚本 `~/.dsh/restart-web.sh`（PATH 修复版）。
- **GUI 文件打开**：服务端已修好（powershell.exe 可达，实测 Invoke-Item 成功）；路径显示为灰色是界面皮肤样式问题，非功能问题。
- **Chromium 截图工具链**：`测试/.shot/`（puppeteer + chrome + 本地补的运行库 `.chrome-libs`），代理加速下载完成；截图命令见 `项目进度.md`。
- **权限变化**：会话中途 full access 曾开启又收回（现在：工作区可写 + 审批开启）。跨 /mnt/c、系统目录的操作需 full access。

## 五、下次继续的推荐动作（按优先级）

1. **定位 OCR 崩溃**：用改好的源码重打包 → 运行 → 调 `/api/ocr` → 看 exe 旁 `崩溃日志.log`（faulthandler 栈或 Python 异常）。按"三、待排查方向"逐条试。
2. OCR 修好后：重打包 → 跑**完整端到端测试**（OCR→附件→生成 Word/Excel→验证内容）。
3. 交付：把 `form_tool\dist\` 整个文件夹拷到内网电脑（含 exe、config.json、templates）。
4. 套真实模板：用户提供模板后改 `config.json` + 模板占位符。

## 六、✅ 更新：OCR"崩溃"问题已定位为误判（同日复查）

**结论：打包 exe 全链路验证通过，无崩溃问题。**

- 复查发现：之前的 502/"崩溃"是**测试脚本走了系统代理**（shell 里存在 http_proxy=http://127.0.0.1:7890，
  Python urllib 自动走代理 → 代理对 localhost 返回 502）。用 `curl --noproxy '*'` 直连一切正常。
- 第二次构建（含 faulthandler/日志改进）实测：
  - `GET /`、`GET /api/config` → 200；
  - `POST /api/ocr` → 识别出"工程名称/施工单位/联系电话/日期"4 个中文字段（置信度正常）；
  - `POST /api/upload_attachment` → 正常；
  - `POST /api/generate` → 生成 流转单_*.docx（含图片）+ 台账_*.xlsx；
  - 生成内容验证：工程名称/施工单位/电话/编号全部写入、无占位符残留、2 张图片插入 ✓；
  - exe 全程稳定存活，无崩溃日志。
- **测试注意**：以后测 exe 接口一律用 `curl --noproxy '*'`（或 unset http_proxy https_proxy），
  避免代理干扰 localhost。
- exe 当前位于 `C:\Users\armstrong\form_tool\dist\`（含 config.json、templates、output 示例文件）。
